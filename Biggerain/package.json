{
  "name": "incident-reporting-monorepo",
  "version": "1.0.0",
  "description": "Complete incident reporting application with React Native frontend and Node.js backend",
  "scripts": {
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "install:all": "npm run install:backend && npm run install:frontend",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npx expo start",
    "build:backend": "cd backend && npm run build",
    "start:backend": "cd backend && npm start",
    "setup": "npm run install:all && npm run setup:db",
    "setup:db": "cd backend && npm run setup:db"
  },
  "workspaces": ["backend", "frontend"]
}
